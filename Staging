file { '/bannerstage': 
      ensure => directory,
      owner => 'oracle',
      group => 'dba', 
}
exec { "replace_share": 
      command => "/bin/sed -i 's/spartandrive/bannernfs/g' /etc/fstab",
      onlyif => "/bin/cat /etc/fstab | grep -i BannerStage",
      require => File["/bannerstage"],
}
